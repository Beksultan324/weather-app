import { DateToEmojiPipe } from './date-to-emoji.pipe';

describe('DateToEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
